DocuVault is a Python desktop application that enables users to upload, and parse documents of multiple formats. 

It automatically organizes each document's parsed output into a structured, timestamped folder on disk

This folder contains all sorts of organized information about that inputted document such as metadata, tables, images, and structured content. 

The UI also has several features that make the overall user experience intuitive and convenient. 
